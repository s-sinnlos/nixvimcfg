{
  plugins = {
    # Disable arrow keys and enforc efficient movement
    hardtime.enable = true;
    # Automatic closing of brackets
    nvim-autopairs.enable = true;
    # rainbow brackets
    rainbow-delimiters.enable = true;
    # key hints
    which-key.enable = true;
  };
}
